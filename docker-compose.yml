services:
  app:
    build: .
    image: spring-boot-cicd:latest
    ports:
      - "${APP_PORT:-8080}:${SERVER_PORT:-8080}"
    environment:
      - SERVER_PORT=${SERVER_PORT:-8080}
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-}
